for zone_id in {% for zone in cloudflare_zones %}{{ zone }} {% endfor %}; do

curl --header 'X-Auth-Email: {{ cloudflare_email }}' --header 'X-Auth-Key: {{ cloudflare_token }}' --header 'Content-Type: Application/json'  https://api.cloudflare.co/client/v4/zones/${zone_id}/rulesets

done