- name: Install plugin
  include_tasks: '{{ ansible_os_family }}.yml'

- name: Enable rtmp (straight)
  become: true
  when: not multistream_server__use_ssl
  block:
    - name: Add to config file
      notify: restart nginx service
      ansible.builtin.blockinfile:
        marker: "# {mark} ANSIBLE CONFIG -- rtmp server"
        dest: /etc/nginx/nginx.conf
        block: "{{ lookup('template', 'rtmp_site.j2') }}"

- name: Enable rtmp (straight)
  become: true
  when: multistream_server__use_ssl
  block:
    - name: Add to config file
      notify: restart nginx service
      ansible.builtin.blockinfile:
        marker: "# {mark} ANSIBLE CONFIG -- rtmp server"
        dest: /etc/nginx/nginx.conf
        block: "{{ lookup('template', 'rtmp_site_ssl.j2') }}"

- name: Allow RTMP through firewall
  become: true
  when: not multistream_server__use_ssl
  block:
    - name: Allow RTMP through firewall
      when: not multistream_server__use_ufw
      ansible.posix.firewalld:
        immediate: true
        port: 1935/tcp
        permanent: true
        state: enabled

    - name: Allow RTMP through firewall (ufw)
      when: multistream_server__use_ufw
      community.general.ufw:
        port: 1935
        rule: allow
        proto: tcp
        comment: 'Allow rtmp through firewall'

- name: Allow RTMP through firewall
  when: multistream_server__use_ssl
  become: true
  block:
    - name: Allow RTMP through firewall
      when: not multistream_server__use_ufw
      ansible.posix.firewalld:
        immediate: true
        port: '{{ item }}/tcp'
        permanent: true
        state: enabled
      loop:
        - 1935
        - 1936

    - name: Allow RTMP through firewall (ufw)
      when: multistream_server__use_ufw
      community.general.ufw:
        port: '{{ item }}'
        rule: allow
        proto: tcp
        comment: 'Allow rtmp port ({{ item }}) through firewall'
      loop:
        - 1935
        - 1936


- name: Get cert
  when: multistream_server__use_ssl
  block:
    - name: Attempt to get cert
      register: certbot_call
      ansible.builtin.shell:
        cmd: >
          /root/.local/bin/certbot certonly
          -v
          --manual --manual-auth-hook /etc/letsencrypt/acme-dns-auth.py
          --preferred-challenges dns
          --debug-challenges
          --non-interactive
          --agree-tos
          -d {{ multistream_server__fqdn }} 2>&1 | tee -a /var/tmp/certbot_output_{{ multistream_server__fqdn }}
      failed_when: "'Certbot failed to authenticate some domains' in certbot_call.stdout"
      changed_when: >
        'Successfully received certificate.' in certbot_call.stdout and
        'skipped, since /etc/letsencrypt/live/' + multistream_server__fqdn + '/privkey.pem exists' not in certbot_call.stdout and
        certbot_call.rc != 0
      args:
        creates: /etc/letsencrypt/live/{{ multistream_server__fqdn }}/privkey.pem

- name: Setup push_script
  become: true
  block:
    - name: Create push script
      ansible.builtin.template:
        dest: /usr/local/bin/push-to-rtmps
        src: push_to_rtmps.sh.j2
        owner: root
        group: root
        mode: "0755"

- name: Setup ffmpeg log file and logrotate policy
  become: true
  block:
    - name: Ensure ffmpeg log directory exists
      ansible.builtin.file:
        path: '{{ multistream_server__ffmpeg_log_dir }}'
        state: directory
        owner: '{{ multistream_server__ffmpeg_log_owner }}'
        group: '{{ multistream_server__ffmpeg_log_group }}'
        mode: "0755"

    - name: Ensure ffmpeg log file exists
      ansible.builtin.file:
        path: '{{ multistream_server__ffmpeg_log_dir }}/{{ multistream_server__ffmpeg_log_file }}'
        state: touch
        owner: '{{ multistream_server__ffmpeg_log_owner }}'
        group: '{{ multistream_server__ffmpeg_log_group }}'
        mode: "0640"

    - name: Install logrotate rule for ffmpeg push logs
      ansible.builtin.template:
        src: ffmpeg_push.logrotate.j2
        dest: /etc/logrotate.d/multistream-ffmpeg-push
        owner: root
        group: root
        mode: "0644"
